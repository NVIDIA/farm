# Library Folder

This folder contains modules that will eventually exist external to the `web/` folder. For simplicity, they live here
now. However, they will be published on an artifactory registry in the future and then pulled into the project in a
versioned state.
